<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/row"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?android:attr/selectableItemBackground"
    android:padding="@dimen/default_margins">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="@dimen/avator_size"
        android:layout_height="@dimen/avator_size"
        android:scaleType="fitCenter"
        tools:src="@drawable/bulls" />

    <TextView
        android:id="@+id/organisation_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/default_margins"
        android:layout_marginStart="@dimen/default_margins"
        android:layout_toEndOf="@id/logo"
        android:layout_toRightOf="@+id/logo"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textSize="@dimen/list_big_letter"
        tools:text="Item Title" />

    <TextView
        android:id="@+id/organisation_location"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/organisation_name"
        android:layout_marginLeft="@dimen/default_margins"
        android:layout_marginStart="@dimen/default_margins"
        android:layout_toEndOf="@id/logo"
        android:layout_toRightOf="@+id/logo"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textColor="@color/medium_grey"
        android:textSize="@dimen/list_small_letter"
        tools:text="Item Description" />

</RelativeLayout>